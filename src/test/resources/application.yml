spring:
  task:
    scheduling:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false

orlen:
  schedule:
    cron: "0 0 6 * * *"
    zone: "Europe/Warsaw"
  bootstrap:
    enabled: false
